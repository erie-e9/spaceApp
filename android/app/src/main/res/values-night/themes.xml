<resources>
    <!-- Main dark theme -->
    <style name="Theme.App.Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Other theme attributes -->
        <item name="lottieBackgroundColor">@color/dark</item>
        <item name="android:windowBackground">@color/dark</item>
    </style>

     <!-- Screen theme for dark mode -->
    <style name="SplashScreenTheme_Dark" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
