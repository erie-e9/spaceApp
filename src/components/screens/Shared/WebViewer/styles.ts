import WebView from 'react-native-webview';
import styled from 'styled-components/native';

export const AccessibilityView = styled.View``;

export const StyledWebView = styled(WebView)`
  flex: 1;
`;
